{
  "private": true,
  "name": "git-flight-rules",
  "version": "1.0.0",
  "description": "Flight rules for git",
  "scripts": {
    "test": "doctoc README.md --github"
  },
  "devDependencies": {
    "doctoc": "^1.3.0",
    "pre-commit": "^1.2.2"
  }
}
